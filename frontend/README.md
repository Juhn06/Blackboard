
  # BlackBoard project setup

  This is a code bundle for BlackBoard project setup. The original project is available at https://www.figma.com/design/HAt0WT7goB6B7pu8nhEzoX/BlackBoard-project-setup.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  